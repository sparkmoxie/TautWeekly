TautWeekly for Plex native Linux platform changelog

v1.4.0
- replaces native private Serve with password-gated public HTTPS Funnel while Manager remains on loopback
- uses the root-owned socket service only for fixed inspect, enable, disable, public DNS, and trusted-TLS verification operations
- requires exact Funnel cleanup before explicit stop, update, access reset, adapter revocation, or uninstall can proceed
- preserves configuration, password verification material, history, previews, schedule, and selected access state across upgrade
- packages the verified Manager social preview and keeps malformed, conflicting, unpublished, or untrusted routes out of Active state

v1.3.4
- optimizes all 32 email GIFs at twice their email display size while preserving frames, timing, loops, and transparency
- refreshes every shipped persistent asset and the preview mirror on bundle change or legacy migration; preserves custom-only files and same-bundle restart edits
- leaves PNG artwork, dynamic media, configuration, private data, and the accepted email layout unchanged

v1.3.3
- displays main TV episode IMDb numbers and show-level fallback IMDb numbers at 12px, matching footer rating text
- preserves the existing score weight, icon dimensions, spacing, and all v0.21.2 footer/watched-marker refinements

v1.3.2
- refines footer typography, including 12px IMDb scores and 27px/800/1.1 primary summary values
- displays unchanged watched circles at 16x16 with 8px spacing only in main movie content; preserves the 26x26 desktop shield
- removes footer watched markers and preserves deliberate recap spacing, dynamic data, privacy, and lifecycle behavior

v1.3.1
- marks movies only from the recipient's qualifying all-time Tautulli history, never another user's activity
- uses the supplied desktop poster shield and consistently spaced, vertically centered title circles with accessible Watched labels
- preserves all newsletter lifecycle, Trending/preheader, Top Genre, Binge Champion, and TV behavior
- verifies the Tautulli v2.18.0 history contract, partial-status exclusion, pagination, and invalid-metadata recovery

v1.3.0
- adds the privacy-preserving server-wide Top Genre footer with approved animated artwork and a neutral movie fallback
- makes Trending weeks show up to four recent movies plus new TV or, when no new TV exists, recent TV strictly newer than one month
- keeps the email count line and preview text synchronized with the Trending shelf contents
- normalizes Binge Champion and Top Genre supporting typography across split and full-width cards

v1.2.2
- Adds a verified stable-package updater to the native tautweekly wrapper while preserving /var/lib/tautweekly.
- Makes the Manager GUI the documented setup source and records the exact check, backup, upgrade, and acceptance flow.
- Adds confirmed per-file deletion for obsolete Manager configuration backups.

v1.2.1
- makes the authenticated Manager endpoint and Config workflow explicit in the shipped Linux landing page
- returns expert setup and verification fallbacks to Manager for normal verification, previews, TestEmail, and scheduling

v1.2.0
- Adds amd64 and arm64 native Manager binaries and selects the matching payload during install or upgrade.
- Makes the authenticated, capability-aware Manager the primary setup, verification, preview, TestEmail, scheduling, and administration surface.
- Defaults Manager access to host loopback with documented SSH-tunnel and explicit TLS reverse-proxy policies.
- Adds explicit one-time bootstrap and narrow access-recovery commands without exposing generated tokens in logs.
- Preserves independent newsletter scheduling and grants an active delivery up to the documented systemd shutdown grace.
- Adds a native Linux preview landing page instead of inheriting Docker/Unraid setup language.
- Documents checksum verification, backup, upgrade, post-upgrade GUI checks, rollback, reinstall, and data-preserving uninstall.
- Makes expert setup and verification helpers use Linux service language, host-local service routing, and the configured preview listener.
- Requires ImageMagick explicitly because verification and newsletter asset handling depend on it.
- Keeps personal TV statistics grouped by show with aggregate watch duration while preferring show-level IMDb, then RT critic, then RT audience.
- Omits absent personal movie/TV rating lines instead of rendering an unavailable placeholder.
- Prints the ordered Plex and Tautulli metadata-readiness checklist after first setup and relevant package updates.
- Distinguishes direct-Plex connectivity verification from upstream metadata freshness before PreviewAll or TestEmail.
- Documents per-library scope, potentially long Plex refreshes, and the safe routine-update exception.
- Prefers exact-episode IMDb on TV release rows and falls back to that episode's RT critic or audience score only when IMDb is absent.
- Rejects show-level, movie, TMDB, TVDB, unknown, and unlabelled episode substitutes while preserving score-dependent RT icons.
- Recovers provider-labelled movie RT and exact-episode IMDb from authenticated native Plex XML when JSON omits those target provider entries.
- Reports an empty optional hosted exact-match fallback as informational instead of a ratings warning.
- Ships the exact approved TautWeekly product mark and favicon on the private preview landing page while retaining native Linux platform identity.
- Leaves generated newsletters and their existing mail-state artwork unchanged.
- Explicitly requests Plex's optional Rating element so available movie RT and exact-episode IMDb values are not hidden by a flattened selected IMDb/TMDB score.
- Identifies Plex Movie library Ratings Source > Rotten Tomatoes and refreshed metadata as prerequisites for intended RT output.
- Recommends direct Plex during setup for full rating and artwork fidelity while preserving Tautulli-only fallback operation.
- Documents that verification tests the renderer's resolved direct Plex URL/token through identity and authenticated library requests.
- Explains the service-network boundary and keeps the Plex token out of URLs, process arguments, and verification output.
- Renders recognized selected IMDb, TMDB, and TVDB ratings from either Tautulli rating field pair.
- Makes Tautulli field discovery compatible and explicitly requests provider-labelled rating fields.
- Accepts valid minimal rating-only JSON exports instead of rejecting responses under 64 bytes.
- Displays recovered show IMDb ratings on TV release cards.
- Verifies that SendTest delivers recovered RT and IMDb scores in the decoded MIME message.
- Fixes Tautulli single-item RT fallback requests and parses rating-only JSON exports.
- Limits that fallback to metadata level 1 with media information disabled.
- Preserves the dark-first email palette with Apple-compatible light/dark metadata, explicit longhand backgrounds, and table color fallbacks.
- Supplies Plex's required deleted-history title and episode-index match context, validates responses, and keeps missing artwork nonfatal.
- Retries empty modern Plex and legacy TMDB/TVDB exact matches through Plex's current provider POST contract.
- Recovers deleted Plex history through legacy TMDB/TVDB exact identifiers and reports unsupported or empty matches safely.
- Fixes deleted-history poster recovery on Unix by using a portable generic-poster probe.
- Restores IMDb ratings inside watched-TV personal statistics, including deleted-history shows.
- Restores missing movie RT and TV IMDb scores from Plex's exact tokenless public rating page.
- Recovers deleted Plex history posters and metadata from Tautulli's retained exact GUID.
- Treats sparse Tautulli hero metadata as optional during previews and test sends.
- Accepts selected-library recently-added rows when Plex omits the redundant section ID.
- Limits HOT NEW RELEASE to movies and falls back to Trending when no new movie exists.
- Lists up to four most-watched movies and TV shows, omitting an empty TV card.
- Inherits the supplied movie/TV stat GIFs through the maintained container payload.
- Splits Binge Champion into watch duration plus nonzero unique movie/TV-show counts.
- Removes qualifying-play copy from Total Watched.
- Adds a read-only stable-release check with no periodic or unattended updater.
- Locks upgrades against concurrent preview/send work and verifies release metadata/service recovery.
- Resolves the configured IANA timezone explicitly for every schedule decision.
- Refuses invalid timezones instead of silently sending on UTC.
- Reports the active scheduler timezone, local time, and UTC offset from its heartbeat.
- Adds setup and standalone global movie/TV library selection.
- Scopes releases, quiet mode, Trending, Binge Champion, and personal statistics upstream.
- Loads the exclusion roster from the two bulk Tautulli user endpoints.
- Falls back to the bulk roster when a single-user lookup is rejected.

v1.0.0
- Added native PowerShell 7 operation without Docker.
- Added a hardened systemd service and protected service account.
- Added guided setup, preview, test, exclusion, scheduling, backup, and upgrade commands.
- Defaulted the preview listener to localhost and kept private data outside the application directory.
